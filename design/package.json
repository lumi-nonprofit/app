{
  "name": "lumi-design-tokens",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Single source of Lumi design tokens; Style Dictionary codegen for RN (JS), CSS, iOS (Swift), Android (Compose). One token source feeds every UI surface (ADR 0001 §2.3, §7).",
  "scripts": {
    "build": "style-dictionary build --config style-dictionary.config.js"
  },
  "devDependencies": {
    "style-dictionary": "^4.0.0"
  }
}
